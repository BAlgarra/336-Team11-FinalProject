<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Collections Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
    <link href="/css/styles.css" rel="stylesheet" />
    <link rel="icon" type="image/png" href="/img/comicFavIcon.png" />
</head>

  </head>
  <body>
    <%- include('partials/header') %> <%- include('partials/navbar') %>

    <div class="container mt-4">
      <h2 class="mb-4"><%= collectionName %>

    <!-- DELETE COLLECTION BUTTON -->
    <div class="d-flex justify-content-end mb-3">
      <a 
      href="/deleteCollection/<%= collection_id %>" 
      class="btn btn-danger"
      onclick="return confirm('Are you sure you want to delete this entire collection?');"
      >
      Delete Collection
      </a>
    </div>

      <div class="row">
        <% comics.forEach((comic, index) => { %>

        <!-- Comic Card from bootstrap-->
        <div class="col-6 col-md-3 mb-4">
          <div class="card shadow-sm">
            <!-- Card Cover Image (made clickable for modal) -->
            <img
              src="<%= comic.cover_image_url %>"
              class="card-img-top"
              role="button"
              data-bs-toggle="modal"
              data-bs-target="#comicModal<%= index %>"
              alt="Comic Cover"
            />

            <div class="card-body">
              <h5 class="card-title">
                <%= comic.title || ("Issue #" + comic.issue_num) %>
              </h5>

              <p class="card-text">
                <small class="text-muted"><%= comic.volume_name %></small>
              </p>
            </div>
          </div>
        </div>

        <!-- Comic Detail Modal From bootstrap -->
        <div
          class="modal fade"
          id="comicModal<%= index %>"
          tabindex="-1"
          aria-hidden="true"
        >
          <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
              <!-- Modal Header -->
              <div class="modal-header">
                <h5 class="modal-title">
                  <%= comic.title || ("Issue #" + comic.issue_num) %>
                </h5>
                <button class="btn-close" data-bs-dismiss="modal"></button>
              </div>

              <!-- Modal Body -->
              <div class="modal-body">
                <!-- One top image -->
                <div class="text-center mb-4">
                  <img
                    src="<%= comic.cover_image_url %>"
                    class="img-fluid rounded"
                    alt="Comic Cover"
                  />
                </div>

                <!-- Comic Info (Can be edited down later) -->
                <div class="container">
                  <p>
                    <strong>ComicVine ID:</strong> <%= comic.comicvine_id %>
                  </p>

                  <p><strong>Title:</strong> <%= comic.title %></p>

                  <p><strong>Issue Number:</strong> <%= comic.issue_num %></p>

                  <p><strong>Volume ID:</strong> <%= comic.volume_id %></p>

                  <p><strong>Volume Name:</strong> <%= comic.volume_name %></p>

                  <p><strong>Cover Date:</strong> <%= comic.cover_date %></p>

                  <p><strong>Store Date:</strong> <%= comic.store_date %></p>

                  <p>
                    <strong>Cover Image URL:</strong><br />
                    <a href="<%= comic.cover_image_url %>" target="_blank">
                      <%= comic.cover_image_url %>
                    </a>
                  </p>

                  <p>
                    <strong>Super Image URL:</strong><br />
                    <a href="<%= comic.image_super_url %>" target="_blank">
                      <%= comic.image_super_url %>
                    </a>
                  </p>

                  <p>
                    <strong>Thumbnail URL:</strong><br />
                    <a href="<%= comic.image_thumb_url %>" target="_blank">
                      <%= comic.image_thumb_url %>
                    </a>
                  </p>

                  <p>
                    <strong>API Detail URL:</strong><br />
                    <a href="<%= comic.api_detail_url %>" target="_blank">
                      <%= comic.api_detail_url %>
                    </a>
                  </p>

                  <p>
                    <strong>Site Detail URL:</strong><br />
                    <a href="<%= comic.site_detail_url %>" target="_blank">
                      <%= comic.site_detail_url %>
                    </a>
                  </p>

                  <hr />

                  <p><strong>Description:</strong></p>
                  <div>
                    <%- comic.description || "<em>No description available.</em>" %>

                  </div>
                </div>
              </div>

              <!-- Modal Footer -->
              <a
                href="/deleteCollectionItem/<%= collection_id %>/<%= comic.comic_id %>"
                class="btn btn-danger"
              >
                Remove from Collection
              </a>
              <hr>
            </div>
          </div>
        </div>

        <% }); %>
      </div>
    </div>
  </body>
</html>
